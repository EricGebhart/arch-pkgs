# Maintainer: Eric Gebhart <e.a.gebhart@protonmail.com>
pkgname=emacs-apps
pkgver=1
pkgrel=1
pkgdesc="Everything I need for emacs"
arch=(any)
url="https://github.com/ericgebhart/arch-pkgs"
license=(MIT)
groups=(eagebhart)

### Emacs
### My emacs config, mbsync/isync config, notmuch, etc.
depends=(emacs isync notmuch languagetool hunspell mu-git)

rootdir=$PWD
