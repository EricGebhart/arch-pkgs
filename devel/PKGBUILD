# Maintainer: Eric Gebhart <e.a.gebhart@protonmail.com>
pkgname=devel
pkgver=1
pkgrel=1
pkgdesc="Stuff for all the programming languages I use."
arch=(any)
url="https://github.com/ericgebhart/arch-pkgs"
license=(MIT)
groups=(eagebhart)

#### code
depends+=(linux-headers leiningen clojure jdk-openjdk perl python ctags openscad ruby ruby-bundler lua guile racket)

rootdir=$PWD
