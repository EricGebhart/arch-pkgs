# Maintainer: Eric Gebhart <e.a.gebhart@protonmail.com>
pkgname=audio
pkgver=1
pkgrel=1
pkgdesc="Audio stuff"
arch=(any)
url="https://github.com/ericgebhart/arch-pkgs"
license=(MIT)
groups=(eagebhart)

# audio
# alsa - is a group. See Makefile.
depends=(pulseaudio pulseaudio-alsa pulseaudio-bluetooth)

rootdir=$PWD
